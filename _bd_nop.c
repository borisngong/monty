#include "monty.h"
/**
 * _bd_nop - does nothing
 * @stack: represents a pointer to the stack.
 * @line_number: represents the line number where the nop function is called
 * Return: always void
 */
void _bd_nop(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
