#include "monty.h"

/**
 * _bd_queue - reponsible for setting the format of the data to a queue
 * @stack: represents a pointer to the stack.
 * @line_number: represents the line number where the queue function is called.
 */
void _bd_queue(stack_t **stack, unsigned int line_number)
{
	(void) stack;
	(void) line_number;
	arg_data->stack = 0;
}
